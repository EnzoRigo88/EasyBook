FROM golang:1.22-alpine

RUN apk add --no-cache git && \
    go install github.com/air-verse/air@latest

WORKDIR /app

COPY go.mod go.sum ./
RUN go mod download

# Source is mounted as a volume at runtime; this layer just pre-warms deps.
COPY . .

EXPOSE 3000

CMD ["air", "-c", ".air.toml"]
