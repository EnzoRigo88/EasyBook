/**
 * App.tsx — placeholder del dashboard.
 *
 * Próximos pasos reales (Fase 2):
 * - Página de calendario de reservas (pages/Bookings.tsx)
 * - Página de configuración de canchas (pages/Courts.tsx)
 * - Login con JWT contra el control-plane
 * - Cliente HTTP (fetch wrapper) hacia /api/v1
 */
export default function App() {
  return (
    <div style={{ fontFamily: "system-ui", padding: 40 }}>
      <h1>🎾 EasyBook Dashboard</h1>
      <p>Placeholder — listo para construir el calendario de reservas.</p>
    </div>
  );
}
